# labproject_OOP
The Restaurant Ordering System is an OOP-based console application developed in C++ that automates the food-ordering workflow inside a restaurant. It allows customers to browse menu categories, select items, place orders, and generate digital receipts. The system uses core OOP concepts such as abstraction, encapsulation, inheritance.
